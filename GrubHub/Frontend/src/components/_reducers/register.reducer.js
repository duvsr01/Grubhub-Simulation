const initialState = {
  isAuthenticated: false,
  user: {}
};

export const registerReducer = function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};
